{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "ConnectionStrings": {
    "lojaConnectionString": "-- usar variável de ambiente ConnectionStrings__lojaConnectionString --"
  },
  "dependencies": {
    "broker": {
      "topic": "-- usar variável de ambiente dependencies__broker__topic --",
      "producer": {
        "BootstrapServers": "-- usar variável de ambiente dependencies__broker__producer__BootstrapServers --"
      }
    },
    "redis": "-- usar variável de ambiente dependencies__redis --"
  }
}
