CREATE STREAM pedidos (produtoId VARCHAR, quantidade INTEGER)
  WITH (kafka_topic='pedido', key_format='kafka', value_format='json', partitions=2, replicas=2);

CREATE STREAM notificacoes (origem VARCHAR, mensagem VARCHAR)
  WITH (kafka_topic='notificacao', key_format='kafka', value_format='json', partitions=2, replicas=2);
